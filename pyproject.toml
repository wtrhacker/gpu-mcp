[project]
name = "gpu-mcp"
version = "0.0.0"
description = "Config-driven GPU cluster MCP server experiments and contracts."
requires-python = ">=3.11"
dependencies = [
    "fabric",
    "mcp",
    "paramiko",
]

[project.optional-dependencies]
test = [
    "jsonschema",
    "pytest",
]
